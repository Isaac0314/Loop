# Normalise line endings to LF in the repository for cross-platform consistency.
* text=auto eol=lf

# Treat common binaries as binary (never normalise).
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.pdf  binary
