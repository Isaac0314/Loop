# Optional. By default the app calls the API same-origin at /api (Vite proxies
# it in dev; nginx proxies it in the Docker build). Set this only if you want to
# point the frontend at a backend on a different origin.
# VITE_API_BASE=http://localhost:8000
