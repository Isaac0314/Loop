[pytest]
# Anchor the rootdir at backend/ and put it on sys.path so the tests can
# `from app...` regardless of how pytest is invoked (bare `pytest` in CI vs
# `python -m pytest` locally).
pythonpath = .
testpaths = tests
